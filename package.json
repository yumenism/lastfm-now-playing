{
  "name": "playwright-vercel",
  "version": "1.0.0",
  "scripts": {
    "dev": "vercel dev", 
    "deploy": "vercel --prod" 
  },
  "dependencies": {
    "playwright": "^1.23.0",
    "playwright-aws-lambda": "^1.0.0"
  },
  "engines": {
    "node": "16.x"
  }
}
